# Always install devDependencies — the deploy build needs `prisma`, `typescript`
# and the `@types/*` packages even when NODE_ENV=production on the host (Railway,
# Render, Nixpacks). Without this the `tsc` / `prisma generate` build step fails.
production=false
