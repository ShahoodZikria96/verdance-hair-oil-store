# Always install devDependencies — the deploy build needs `typescript` and the
# `@types/*` packages even when NODE_ENV=production on the host (Railway, Render,
# Nixpacks). Without this the `tsc` build step fails and dist/ is never produced.
include=dev
